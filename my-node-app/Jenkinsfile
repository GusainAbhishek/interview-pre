pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh 'docker build -t my-node-app:latest .'
        sh 'docker push myregistry/my-node-app:latest'
      }
    }
    stage('Deploy') {
      steps {
        sh 'kubectl apply -f k8s/deployment.yaml'
      }
    }
  }
}
