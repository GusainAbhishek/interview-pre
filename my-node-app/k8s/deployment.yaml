apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-node-app
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: my-node-app
          image: gusaindoc/node:latest
          # <--- NO env section! App crashes on startup
